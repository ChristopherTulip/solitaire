module.exports = function(grunt) {

  // Project configuration.
  grunt.initConfig({
    pkg: grunt.file.readJSON('package.json')
  });

  // Jade
  // grunt.loadNpmTasks('grunt-contrib-jade');

  // Default task(s).
  // grunt.registerTask('default', ['grunt-contrib-jade'])

};
